---
title: "Семантические поля"
output: html_document
---

```{r, include=FALSE}
source("shared.R")
```

```{r render table}
ordered_linkers <- linkers[order(linkers$semfield1), ]
ordered_linkers %>% select(semfield1,subsense1,linker,source,pos,sense,ptclass,other_senses,other_pos,semfield2,subsense2) -> ordered_linkers

reactable(ordered_linkers,
          defaultPageSize = 100,
          # filterable = TRUE,
          searchable = FALSE, 
          showSortable = TRUE, 
          showSortIcon = TRUE,
          groupBy = c("semfield1", "linker"),
          columns = list(
            ptclass = colDef(show = FALSE),
            other_senses = colDef(show = FALSE),
            other_pos = colDef(show = FALSE),
            subsense2 = colDef(show = FALSE),
            semfield1 = colDef(name = "Семантическое поле"),
            subsense1 = colDef(name = "Подкласс",
                               filterInput = filterList),
            semfield2 = colDef(show = FALSE),
            linker = colDef(name = "Коннектор",
                            filterable = TRUE),
            source = colDef(name = "Источник",
                            filterable = TRUE,
                            filterInput = filterList),
            pos = colDef(name = "Часть речи",
                         filterable = TRUE,
                         filterInput = filterList),
            sense = colDef(name = "Значение")
          ),
          details = function(index) { detailsFunc(index, ordered_linkers) },          
          elementId = "linkertbl3")
```

